- apiVersion: htnn.mosn.io/v1
  kind: Consumer
  metadata:
    name: spacewander
    namespace: default
  spec:
    auth:
      keyAuth:
        config:
          key: xx
    filters:
      demo:
        config:
          hostName: darwin
